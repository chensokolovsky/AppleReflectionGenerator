{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "pwa-chrome",
      "request": "launch",
      "name": "Launch Chrome to localhost",
      "url": "http://localhost:5500/tool/index.html",     // adjust port/path
      "webRoot": "${workspaceFolder}"
    }
  ]
}